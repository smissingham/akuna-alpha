# `akuna_core::detection`

File-type detection using the [Google Magika ML model](https://github.com/google/magika)
with native Burn backends. Exposed behind the `detection` feature of `akuna-core`.

{{#include ../../src-crates/core/src/detection/README.md}}
