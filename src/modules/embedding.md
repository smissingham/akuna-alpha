# `akuna_core::embedding`

Text embedding models (MiniLM, BGE, MPNet, BGE-M3) with Burn + WGPU
backends. Exposed behind the `embedding` feature of `akuna-core`.

{{#include ../../src-crates/core/src/embedding/README.md}}
