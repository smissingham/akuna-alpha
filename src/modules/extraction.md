# `akuna_core::extraction`

File content + metadata extraction with type-driven dispatch. Exposed behind
the `extraction` feature of `akuna-core`.

{{#include ../../src-crates/core/src/extraction/README.md}}
