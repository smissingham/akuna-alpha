# `akuna_core::reranking`

Cross-encoder text rerankers (BGE-reranker-base, Jina-reranker-v2) with
Burn + WGPU backends. Exposed behind the `reranking` feature of `akuna-core`.

{{#include ../../src-crates/core/src/reranking/README.md}}
