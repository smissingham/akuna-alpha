# `akuna_core::chunking`

Text chunking via `memchunk` with optional `tree-sitter` grammar-aware
chunking. Exposed behind the `chunking` feature of `akuna-core`.

{{#include ../../src-crates/core/src/chunking/README.md}}
